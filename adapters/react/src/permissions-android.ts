// PermissionsAndroid moved to @symbiote/engine (a framework-agnostic device-state module
// re-exported by every adapter). This thin re-export keeps @symbiote/react's surface identical.
export { PermissionsAndroid, PERMISSIONS, RESULTS } from '@symbiote/engine';
export type { IPermission, IPermissionStatus, IRationale } from '@symbiote/engine';
