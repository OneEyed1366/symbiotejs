// Appearance moved to @symbiotejs/engine (a framework-agnostic device-state module re-exported
// by every adapter). This thin re-export keeps @symbiotejs/react's public surface identical.
export { Appearance } from '@symbiotejs/engine';
export type { IColorSchemeName, IColorSchemePreference } from '@symbiotejs/engine';
