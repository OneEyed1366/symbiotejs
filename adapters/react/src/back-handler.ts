// BackHandler moved to @symbiote/engine (a framework-agnostic device-state module re-exported
// by every adapter). This thin re-export keeps @symbiote/react's public surface identical.
export { BackHandler } from '@symbiote/engine';
export type { IBackPressEventName, IBackPressHandler } from '@symbiote/engine';
