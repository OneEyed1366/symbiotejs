---
"@symbiote-native/splash-screen": patch
---

Add `@symbiote-native/splash-screen`, wrapping `react-native-bootsplash` behind the one-dependency native proxy pattern with framework-agnostic core plus React, Vue, and Angular bindings.
