node_modules/
build/
build-ngc/
dist/
*.tsbuildinfo

# iOS
ios/Pods/
ios/build/
ios/*.xcworkspace/xcuserdata/
ios/*.xcodeproj/xcuserdata/
DerivedData/

# Metro / Hermes
.metro-health-check*
*.jsbundle

# misc
.DS_Store

# debug session artifacts (simulator/device logs, watched process pids)
*.log
*.pid

# raw simulator screen recordings — convert to an optimized gif under assets/ instead
*.mov

# hidden folders are local-only (.docs, .claude, .notes, .vendors, …); dotfiles stay tracked
.*/
# ...except CI config, which must ship to the remote
!.github/
